import styled from 'styled-components'
import Cell from "../../Cell"

export const ColumnCellWrapper = styled(Cell)`
    justify-content: space-between;
    background-color: #f5f5f5;
    border-bottom: none;
`