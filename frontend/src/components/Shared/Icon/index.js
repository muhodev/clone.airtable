import "./style.css";

function Icon(props) {
  let classNames = "icon ";

  switch (props.size) {
    case value:
      break;

    default:
      break;
  }

  return <div className={classNames}>{props.children}</div>;
}
